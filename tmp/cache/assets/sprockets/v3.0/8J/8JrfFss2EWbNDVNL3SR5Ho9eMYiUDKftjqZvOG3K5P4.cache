I"¨(function() {
  var ready;

  ready = function() {
    var adminLessonTemplatesCalendar;
    adminLessonTemplatesCalendar = $("#admin_lesson_templates_calendar");
    return adminLessonTemplatesCalendar.fullCalendar($.extend({}, $.fn.fullCalendar.defaults, {
      columnFormat: 'dddd',
      header: false,
      eventRender: function(event, element) {
        element.data('url', '/admin/lesson_templates/' + event.id);
        return element.attr('id', 'lesson_template_' + event.id);
      },
      eventClick: function(calEvent, jsEvent, view) {
        var $modal;
        WebuiPopovers.hideAll();
        $modal = $('#lessonModal');
        return $modal.find('.modal-content').load('/admin/lesson_templates/' + calEvent.id + '/edit', function() {
          return $modal.modal('show');
        });
      },
      events: adminLessonTemplatesCalendar.data('events-url')
    }));
  };

  $(document).ready(ready);

}).call(this);
:ET